# CLI Tool example
# This demonstrates command-line argument handling

fn greet(name: str): str
    return "Hello, " + name + "!"

fn add(a: int, b: int): int
    return a + b

# Example usage would be:
# slang cli_tool.sl --name World --add 5 3